(jbuild_version 1)

(library
  ((name typed_ruby)
   (public_name typed_ruby)
   (flags (:standard -warn-error A))
   (libraries (core))))

(menhir
 ((flags (--explain))
  (modules (parser))))

(ocamllex (lexer))
